{
  "devDependencies": {
    "@types/bun": "latest"
  },
  "module": "src/server.ts",
  "name": "regexplanet-bun",
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "scripts": {
    "start": "bun run --logLevel=debug dist/server.js",
    "build": "bun build src/server.ts --outdir ./dist"
  },
  "type": "module",
  "dependencies": {
    "@regexplanet/common": "^1.1.0"
  }
}